---
domain: infra
---

# Infra — decisions

### D001 — Three-layer journal: roadmap, domains, features

- Status: accepted
- Context: Legacy entries mixed truth, intent and dated revisions; shipping one feature meant patching 15 entries.
- Decision: Intent lives in `roadmap/`, built truth in `domains/`, execution in `features/`; legacy entries are archived and mined.
- Consequences: A shipped feature updates one `current.md` per domain; context loads by layer.
- Supersedes: —
- Superseded by: —
- From: [[features/context-system/plan]]

^D001

### D002 — Deterministic drift detection, model-assisted correction

- Status: superseded
- Context: Domain context must be corrected periodically without re-reading the codebase.
- Decision: Claims carry machine-checkable anchors and a `verified` commit; a script ranks drift, and a model re-verifies only flagged claims against the diff.
- Consequences: Correction cost scales with code change, not codebase size.
- Supersedes: —
- Superseded by: D027
- From: [[features/context-system/plan]]

^D002

### D003 — Code attribution through `@context` headers

- Status: superseded
- Context: Every TS module already carried a lint-enforced `@context` link to a legacy entry.
- Decision: Headers point at the owning domain or one of its claims; non-TS files are attributed by domain `code` globs.
- Consequences: Archiving legacy entries required rewriting every header in the same change.
- Supersedes: —
- Superseded by: D027
- From: [[features/context-system/plan]]

^D003

### D004 — Deno is a pure runtime; tsgo type-checks against generated Deno types

- Status: accepted
- Context: A root `deno.json` with Deno-side type-checking fights the pnpm workspace: Deno ignores `pnpm-workspace.yaml`, needs its own copy of the catalog, `deno add` overwrites `catalog:` entries, and a root-level resolution error rewrites the root `package.json`. `Deno.*` APIs should still be usable freely.
- Decision: No `deno.json`. pnpm resolves packages, Nx runs tasks, Deno only executes (`deno run` / `deno test` with `--no-check`). `tsgo` is the single type-checker; Deno's declarations come from `deno types`, generated by an Nx target into the `@kuib-ai/deno-types` workspace package and listed in the base tsconfig `types` next to `node`.
- Consequences: Deno and Node global declarations clash inside `.d.ts` files, so `skipLibCheck` stays on. Deno resolves pnpm's symlinked `@kuib-ai/*` packages from source without configuration. Deno commands run per project, never from the root. `denols` never starts in the repo.
- Supersedes: —
- Superseded by: —
- From: [[features/deno-runtime/plan]]

^D004

### D005 — Explicit `.ts` import specifiers

- Status: accepted
- Context: Extensionless relative imports only work in Deno with sloppy-imports resolution, which costs extra filesystem probes per import and a flag on every command.
- Decision: Every relative import names its file (`./unit/index.ts`); `allowImportingTsExtensions` permits it.
- Consequences: Resolution is exact and cheapest; the switch was one large mechanical rewrite.
- Supersedes: —
- Superseded by: —
- From: [[features/deno-runtime/plan]]

^D005

### D006 — Guard the root manifest against Deno's pnpm-workspace migration

- Status: accepted
- Context: Deno 2.9 copies `pnpm-workspace.yaml` into the root `package.json` (`workspaces`, `catalog`, `catalogs`) when a root-level resolution error meets it, silently forking the source of truth.
- Decision: `pnpm run check` first runs a script that fails if any of those keys appear in the root manifest.
- Consequences: The migration can never land unnoticed; `pnpm-workspace.yaml` stays the only workspace and catalog definition.
- Supersedes: —
- Superseded by: —
- From: [[features/deno-runtime/plan]]

^D006

### D007 — pnpm provisions the runtime

- Status: accepted
- Context: Every device in the mesh must run the same runtime version without a system install.
- Decision: `deno` is a root devDependency pinned in the catalog, with its build script allowed, so `pnpm install` places it in `node_modules/.bin` and `pnpm run` scripts find it on `PATH`.
- Consequences: `git clone && pnpm install` is the whole setup; only npm-published Deno versions are available.
- Supersedes: —
- Superseded by: —
- From: [[_archive/architecture-overview/decisions#Runtime & Distribution]]

^D007

### D008 — pnpm strict catalogs and Nx-cached tasks

- Status: accepted
- Context: A monorepo of many small packages needs consistent dependency versions and fast repeated checks.
- Decision: pnpm workspaces with `catalogMode: strict` (catalog entries are added with `pnpm add --save-catalog`, never hand-written), and Nx as the task runner with cached `typecheck`, `lint`, `format`, `test` and `generate` targets.
- Consequences: A version bump happens in one place; unchanged projects skip their checks.
- Supersedes: —
- Superseded by: —
- From: [[_archive/architecture-overview/decisions#Build System]] | [[_archive/house-style-linting/decisions#Decisions]]

^D008

### D009 — Stable TypeScript kept next to tsgo

- Status: accepted
- Context: The native TypeScript preview (`tsgo`) has no classic `lib/typescript.js` API, so `typescript-eslint` cannot load it.
- Decision: `tsgo` type-checks; stable TypeScript 6.0.x stays in the catalog only for `typescript-eslint` and the rule tester.
- Consequences: Two compilers are installed; only `tsgo` gates types.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#Decisions]] | [[_archive/architecture-overview/decisions#Build System]]

^D009

### D010 — The env layer exposes base directories only

- Status: accepted
- Context: Paths for config, data, state, cache and runtime IPC differ per OS, while file names are product decisions.
- Decision: `@kuib-ai/env` returns the five base directories with no app name; the application layer (`@kuib-ai/config`) appends `kuib/` and the file names.
- Consequences: Consumers compose paths such as `join(dirs.config, "kuib", "config.toml")`; one-off path resolvers are replaced by this contract.
- Supersedes: —
- Superseded by: —
- From: [[_archive/application-directories/decisions#Infra API contract (`@kuib-ai/env`)]] | [[_archive/application-directories/decisions#Decision]]

^D010

### D011 — XDG layout on macOS, Known Folders on Windows

- Status: accepted
- Context: kuib is a CLI, TUI and daemon, not a GUI app with a bundle ID. Users of git, neovim, gh and terraform expect `~/.config` on macOS; dotfile managers ignore `~/Library/Application Support`.
- Decision: Linux and macOS both use XDG variables and defaults; Windows uses roaming AppData for config and local AppData for data, state and cache. Non-absolute overrides are ignored.
- Consequences: Dotfiles and mesh setups behave the same on Linux and macOS; the macOS mapping of `dirs`/`env-paths` is deliberately rejected.
- Supersedes: —
- Superseded by: —
- From: [[_archive/application-directories/decisions#macOS — Apple Library vs CLI expectation]] | [[_archive/application-directories/decisions#Windows — Known Folders]]

^D011

### D012 — Development runs live under the workspace `dist/`

- Status: accepted
- Context: Running from source must not write into real user directories.
- Decision: Outside production, every base directory resolves to `<workspace>/dist/<kind>`; an explicit `dev` option overrides.
- Consequences: Dev databases, logs and sockets are disposable and per-checkout; consumers never reimplement the switch.
- Supersedes: —
- Superseded by: —
- From: [[_archive/application-directories/decisions#Infra API contract (`@kuib-ai/env`)]]

^D012

### D013 — One root config with file, env, CLI precedence

- Status: accepted
- Context: kuib ships as one program, so per-package configuration would be over-engineering; secrets must not live in a synced file.
- Decision: One Zod-validated config resolved as `defaults < config.toml < KUIB_* environment < CLI flags`, with root `.env` files loaded for development convenience. Secrets and operational values are returned beside the config, never inside it. Only the app entry bootstraps config and passes it down.
- Consequences: `config.toml` holds persistent non-secret preferences; `.env` holds secrets and temporary overrides and is gitignored.
- Supersedes: —
- Superseded by: —
- From: [[_archive/architecture-overview/decisions#Config & Env Resolution (`@kuib-ai/env`, 2026-07-01)]] | [[_archive/application-directories/decisions#Decision]]

^D013

### D014 — LLM tracing through OpenTelemetry to Phoenix

- Status: accepted
- Context: Model calls were invisible — no view of the messages, tool calls, tokens, latency or errors per turn. AI SDK v7 emits no OTel spans by itself.
- Decision: A separate `@kuib-ai/telemetry` package bridges the AI SDK through `@ai-sdk/otel` and OpenInference into an OTLP exporter for a self-hosted Phoenix. It is opt-in by endpoint and injected — it never reads the environment.
- Consequences: The OpenTelemetry dependency stack stays out of the engine. Phoenix keeps traces local (Langfuse cloud would send prompts and tool results off-box). Exports fail quietly if Phoenix is down.
- Supersedes: —
- Superseded by: —
- From: [[_archive/observability/decisions#Decision]] | [[_archive/observability/decisions#Why not Langfuse]]

^D014

### D015 — House style is a lint plugin at error severity

- Status: accepted
- Context: Agent-written code must match the author's hand-written idiom: one unit per dot.case directory, default-exported units, namespace-object barrels, function expressions, zero prose comments.
- Decision: A custom ESLint plugin encodes those conventions plus reused core rules, all at `error`, in the plugin's own `recommended` config; `eslint.config.ts` only consumes it. The plugin is linted by its own rules.
- Consequences: Violations block `pnpm run check`; exceptions need an explicit `eslint-disable`.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#The house style (audited from the author's hand-written reference codebase)]] | [[_archive/house-style-linting/decisions#Rules (final set)]] | [[_archive/house-style-linting/decisions#Lint infra changes (2026-07-01)]]

^D015

### D016 — Errors as result tuples, not try/catch

- Status: accepted
- Context: The house style handles failures explicitly at the call site.
- Decision: `try`/`catch` is banned by lint; `withError` returns a Go-style `[err, value]` tuple with errors mapped to protocol errors.
- Consequences: Error paths are visible in control flow; the few unavoidable `try` blocks carry an `eslint-disable`.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#The house style (audited from the author's hand-written reference codebase)]]

^D016

### D017 — Relative imports are banned only across packages

- Status: accepted
- Context: A `@/` self-alias does not survive declaration emit, and `eslint-plugin-import-x` needs a native resolver and still did not fire without full configuration.
- Decision: Within-package relative imports stay; a custom rule bans relative imports that escape `packages/*` or `apps/*`, using path arithmetic only. Cross-package imports go through `@kuib-ai/<pkg>`.
- Consequences: The rule is dependency-free and deterministic; literal `@/` aliases are not used.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#RESOLVED: relative imports — cross-package-only ban (2026-07-01)]]

^D017

### D018 — Arrow functions are banned everywhere

- Status: accepted
- Context: Banning only top-level arrows left callbacks and object properties outside the function-expression convention.
- Decision: `house/no-arrow` reports every arrow function and autofixes it to a `function` expression.
- Consequences: Lexical-`this` cases need an explicit `eslint-disable`.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#no-arrow: ban everywhere (2026-07-19)]]

^D018

### D019 — Guard clauses instead of `else`

- Status: accepted
- Context: The house style prefers early exits and explicit dispatch over nested branches.
- Decision: Every `else` is an error; `else if` chains are reported once with a message pointing to `switch`.
- Consequences: Branching code is flat; the escape hatch is `eslint-disable`.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#prefer-guard-clauses rule (2026-07-03)]]

^D019

### D020 — Named union members

- Status: accepted
- Context: Inline object literals in unions hide the variants; the author names each variant and discriminates with an enum.
- Decision: Union members — TypeScript types and Zod schemas alike — must be named; discriminators are named enums rather than bare string-literal unions.
- Consequences: Each variant is its own unit; a tagged union is introduced only when variant shapes diverge.
- Supersedes: —
- Superseded by: —
- From: [[_archive/house-style-linting/decisions#Types: named enums over bare unions; tagged unions only when shapes diverge (2026-07-01)]]

^D020

### D021 — Reload clears the detached runtime

- Status: accepted
- Context: The auto-spawned daemon and engine-service persist across host restarts and keep serving stale code.
- Decision: `pnpm reload` kills both processes and removes their development sockets so the next host run respawns them.
- Consequences: Code changes to the background runtime take effect after one command.
- Supersedes: —
- Superseded by: —
- From: [[_archive/tool-system/decisions#Open / follow-ups]]

^D021

### D022 — Design sessions run in an isolated, converging worktree

- Status: accepted
- Context: Co-design over ssh needs a stable, repeatable environment that never disturbs the main checkout or a live conversation.
- Decision: One idempotent script converges a dedicated worktree, branch and tmux session running `claude "/wireframe"`, never killing a running Claude pane.
- Consequences: Re-running the command is always safe; rebase conflicts are handed to the session instead of aborting.
- Supersedes: —
- Superseded by: —
- From: [[_archive/ux-iteration-process/decisions#Tooling decisions]]

^D022

### D023 — Colocated, hermetic Deno tests

- Status: accepted
- Context: Runner-only verification once hid lint errors, type failures and a cross-file mock leak.
- Decision: Units with runtime behaviour get a colocated `index.test.ts` run by `deno test` through Nx, using `@std/testing/bdd` and `@std/expect`; fakes are injected through parameters, never module mocking; env-derived values are isolated in temp workspaces; lint rules are tested with `@typescript-eslint/rule-tester`. Type-checking is `tsgo`, never the runner.
- Consequences: Barrels, enums, ports and trivial schemas carry no tests; verification means tests plus `pnpm run check`.
- Supersedes: —
- Superseded by: —
- From: [[_archive/testing-strategy/decisions#What gets a test]] | [[_archive/testing-strategy/decisions#Hermeticity rules (each learned from a real failure)]] | [[_archive/testing-strategy/decisions#Package-specific idioms]] | [[features/deno-runtime/plan]]

^D023

### D024 — AGENTS.md and `.agents/` are the single source; tool files are generated

- Status: accepted
- Context: Claude Code, Cursor, Gemini CLI and Antigravity each read instructions, skills, hooks and MCP from different paths and formats; hand-maintained copies had already drifted (duplicated hook scripts, rules split across files).
- Decision: Instructions live only in `AGENTS.md`; skills, MCP and the session hook live in `.agents/`. `scripts/agents.ts` generates each tool's adapters (imports, symlinks, settings JSON) and `pnpm run check` fails when they drift. Codex reads `AGENTS.md` and `.agents/skills` natively.
- Consequences: Adding a tool means adding adapters to the generator. Features a tool lacks (Antigravity session hooks, Gemini compaction re-injection) are documented gaps, not hand-written workarounds.
- Supersedes: —
- Superseded by: —
- From: [[features/agent-harness/plan]]

^D024

### D025 — tmux orchestration with state in the git common dir

- Status: superseded
- Context: The owner runs several agent CLIs in tmux windows, with one orchestrator spawning workers; worktrees are created and managed by the owner.
- Decision: A tool-agnostic `orchestra` script drives tmux (windows, send-keys, capture-pane) and keeps task state in `<git-common-dir>/orchestra/<session>/`, shared by all worktrees and never tracked. Workers notify the orchestrator by typing into its pane. The orchestrator owns the feature plan; workers own the claims of code they change.
- Consequences: Any CLI that takes an initial prompt can be a worker. The orchestrator never creates or removes worktrees or branches.
- Supersedes: —
- Superseded by: D026
- From: [[features/agent-harness/plan]]

^D025

### D026 — Orchestra tasks are journal records; runtime lives in tmux

- Status: superseded
- Context: D025 kept briefs and reports in the git common dir so worktrees could share them. The owner orchestrates inside a single worktree, wants briefs, prompts and review reports kept as project history, and wants the tool in Python rather than shell.
- Decision: Each task is `journal/features/<feature>/tasks/<task-id>/` in the checkout the orchestrator runs in, committed with the work and validated by `journal.ts check`. Pane and window state is read from tmux (window `w:<task-id>`, session option `@orchestra_pane`). The tool is `bin/orchestra`, a PEP 723 uv script; `watch` and `reconcile` derive everything from records plus tmux so they can be re-run at any time.
- Consequences: Tasks always belong to a feature. Orchestrations in different worktrees are independent until the owner merges them.
- Supersedes: D025
- Superseded by: D030
- From: [[features/agent-harness/plan]]

^D026

### D027 — Claims are tied to code by `@claim` links and verified by content hash

- Status: accepted
- Context: Claims listed their code by hand and code pointed back with `@context` headers, so both sides were maintained by hand and a renamed claim meant editing every header. `verified <sha>` tied truth to git history: stamping before a commit made the claim stale on commit, rebased or squashed commits left claims pointing at missing objects, and the sequential `C###` ids collided across parallel branches. Agents rarely saw which truth a function carried while editing it.
- Decision: Code carries the only hand-kept link: a first-line `@claim <domain>[/<claim>]` names what explains a module and `@claim <domain>/<claim>` above a declaration, statement or indented block ties that claim to the scope. `journal.ts stamp` generates the claim's scope sources from the links with a hash of each scope (TS/JS through the syntax tree, other languages by indentation) and dates the claim. Drift compares hashes, not commits. Claim ids are slugs. `check` fails only on structure; `gate` fails while any claim is not fresh, so truth is due at commit.
- Consequences: Links are directives, the one comment the house style allows. Verification works uncommitted, in worktrees and across history rewrites. Migrating put 232 scope links and 214 file links into the code.
- Supersedes: D002, D003
- Superseded by: —
- From: [[features/context-system/plan]]

^D027

### D028 — One-file feature plans and rendered handoffs

- Status: accepted
- Context: `plan.md` and `implementation.toml` listed every item twice, stored derived phase states and two feature states, and had to be kept in sync by hand. Handoffs were hand-written continuation files that repeated state kept elsewhere.
- Decision: A feature is one `plan.md`: items carry `- State:` and optional `- Decisions:`, `- Addresses:`, `- Refs:`; the frontmatter holds the lifecycle and a `checkpoint` (summary, next, blockers, note). Phase state is derived. `journal.ts set` and `checkpoint` write state; `journal.ts handoff` renders the handoff from plan, rulings (`- Ruling:` on decisions), open gaps (`- Recommendation:`), tasks and claims. The only thing written at handoff time is the note. Raw ideas are roadmap items with `state: idea`; there is no inbox.
- Consequences: One storage format for everything in the journal: markdown with YAML frontmatter.
- Supersedes: —
- Superseded by: —
- From: [[features/context-system/plan]]

^D028

### D029 — Agent sync imports MCP edits and refuses other hand edits

- Status: accepted
- Context: `agents sync` was one-way: an MCP server added through a tool's own command, or any hand edit to a generated file, was silently overwritten on the next sync.
- Decision: `sync` records a hash of every file it writes in `.agents/generated.lock.json`. A tool's MCP file that differs from the lock is imported back into `.agents/mcp_config.json` before regenerating; any other file edited outside sync is shown as a diff and left untouched unless `--force`. `check` reports files edited outside sync.
- Consequences: The lock is committed with the adapters.
- Supersedes: —
- Superseded by: —
- From: [[features/agent-harness/plan]]

^D029

### D030 — Orchestra in TypeScript with a plan gate, restarts and watched workers

- Status: accepted
- Context: The Python `bin/orchestra` added a second language and interpreter next to the workspace Deno. Workers had no plan gate, no restart from a log, no acceptance step that updated the plan, and the orchestrator could not see context use, edits outside a task's scope or git writes; its own handoff was hand-written.
- Decision: `tooling/orchestra.ts`, run as `pnpm orchestra`, keeps D026's model (tasks are journal records in the orchestrating checkout, runtime lives in tmux, workers never message the orchestrator) with task state in `brief.md` frontmatter, roles, an optional plan gate (`plan-ready` → `go`), `restart` from `log.md`, `accept` setting plan items implemented, `watch` events for records, idle and context screens, scope and git changes, and an orchestrator `handoff` guard.
- Consequences: The three tools live in the private workspace package `@kuib-ai/tooling` (`tooling/`), which declares their dependencies and is type-checked and linted by Nx; each parses arguments with `@kuib-ai/cli`. One language and runtime for workspace tooling. Watch event lines carry an `orchestra:` prefix so a streaming monitor can tell them from other output.
- Supersedes: D026
- Superseded by: —
- From: [[features/agent-harness/plan]]

^D030
